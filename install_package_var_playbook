- hosts: all
  become: true
  vars:
    package_list:
      - git
      - vim
      - wget
      - unzip
      - tree
  tasks:
    - name: Install packages
      package:
        name: "{{ item }}"
        state: present
      loop: "{{ package_list }}"
